import 'package:flutter/material.dart';

class SettingInfoPage extends StatelessWidget {
  const SettingInfoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("This is the Setting Info Page"),
    );
  }
}
